apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-inference-service
spec:
  template:
    spec:
      containers:
      - name: ai-inference
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
